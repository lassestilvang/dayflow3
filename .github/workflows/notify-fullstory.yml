name: FullStory Integration

on:
  pull_request:
    types: [opened, closed, ready_for_review, merged]
  issues:
    types: [opened, closed, labeled]
  release:
    types: [published"]
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  fullstory-notify:
    runs-on: ubuntu-latest
    if: ${{ secrets.FULLSTORY_API_KEY != '' }}
    steps:
      - name: Send data to FullStory
        run: |
          payload=$(cat <<EOF
          {
            "event": "${{ github.event_name }}_${{ github.event.action }}",
            "properties": {
              "repository": "${{ github.repository }}",
              "sender": "${{ github.actor }}",
              "data": ${{ toJSON(github.event) }}
            },
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          }
          EOF
          )
          
          curl -X POST \
            "https://www.fullstory.com/api/v1/events" \
            -H "Authorization: Basic $(echo -n ${{ secrets.FULLSTORY_API_KEY }}: | base64)" \
            -H "Content-Type: application/json" \
            -d "$payload"