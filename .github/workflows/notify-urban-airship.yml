name: Urban Airship Integration

on:
  pull_request:
    types: [opened, closed, ready_for_review, merged]
  issues:
    types: [opened, closed, labeled]
  release:
    types: [published"]
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  urban-airship-notify:
    runs-on: ubuntu-latest
    if: ${{ secrets.URBAN_AIRSHIP_API_KEY != '' }}
    steps:
      - name: Send data to Urban Airship
        run: |
          payload=$(cat <<EOF
          {
            "event": "${{ github.event_name }}_${{ github.event.action }}",
            "repository": "${{ github.repository }}",
            "sender": "${{ github.actor }}",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "data": ${{ toJSON(github.event) }}
          }
          EOF
          )
          
          curl -X POST \
            "https://go.urbanairship.com/api/events" \
            -H "Authorization: Basic $(echo -n ${{ secrets.URBAN_AIRSHIP_API_KEY }}: | base64)" \
            -H "Content-Type: application/json" \
            -d "$payload"