name: Optimizely Integration

on:
  pull_request:
    types: [opened, closed, ready_for_review, merged]
  issues:
    types: [opened, closed, labeled]
  release:
    types: [published"]
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  optimizely-notify:
    runs-on: ubuntu-latest
    if: ${{ secrets.OPTIMIZELY_API_KEY != '' }}
    steps:
      - name: Send data to Optimizely
        run: |
          payload=$(cat <<EOF
          {
            "event": "${{ github.event_name }}_${{ github.event.action }}",
            "properties": {
              "repository": "${{ github.repository }}",
              "sender": "${{ github.actor }}",
              "data": ${{ toJSON(github.event) }}
            },
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          }
          EOF
          )
          
          curl -X POST \
            "https://api.optimizely.com/v2/events" \
            -H "Authorization: Bearer ${{ secrets.OPTIMIZELY_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "$payload"