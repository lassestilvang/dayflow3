name: Oracle Eloqua Integration

on:
  pull_request:
    types: [opened, closed, ready_for_review, merged]
  issues:
    types: [opened, closed, labeled]
  release:
    types: [published"]
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  eloqua-notify:
    runs-on: ubuntu-latest
    if: ${{ secrets.ELOQUA_API_KEY != '' }}
    steps:
      - name: Send data to Eloqua
        run: |
          payload=$(cat <<EOF
          {
            "event": "${{ github.event_name }}",
            "action": "${{ github.event.action }}",
            "repository": "${{ github.repository }}",
            "sender": "${{ github.actor }}",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "data": ${{ toJSON(github.event) }}
          }
          EOF
          )
          
          curl -X POST \
            "https://secure.p01.eloqua.com/api/REST/2.0/data/github-event" \
            -H "Authorization: Basic ${{ secrets.ELOQUA_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d "$payload"